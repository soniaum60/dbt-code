version: 2
sources:
  - name: datafeed_shared_schema
    database: "{{('DI_SALES')}}"
    schema: dbt_source

    tables:
      - name: STG_CUSTOMERS
      - name: STG_PAYMENTS
      - name: customers
      - name: STG_SUPPLIES_DATA
      - name: STG_ORDERS
        columns:
            - name: id
              tests:
                - unique
                - not_null
            - name: status
              tests:
                - accepted_values:
                      values: ['FAILED', 'placed', 'replaced', 'shipped', 'completed', 'returned', 'return_pending' ]

      - name: STG_PRODUCT_DATA
      - name: emp

  - name: datafeed_shared_schema_dev
    database: "{{('DI_SALES')}}"
    schema: dev
    tables:
      - name: RAW_CUSTOMER
        columns:
        - name: C_CUSTKEY
          tests:
            - unique
            - not_null
        - name: C_NAME
          tests:
            - not_null
      - name: RAW_LINEITEM
      - name: RAW_NATION
      - name: RAW_ORDER




